services:
  - type: web
    name: travel-with-iqlipse
    runtime: node
    buildCommand: npm install && npm install --prefix client && npm run build --prefix client
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
